import { Product } from "@/lib/types";

export const products: Product[] = [];

// Exporting an empty array since categories are now dynamic and managed via the admin panel/database
export const categories: any[] = [];
